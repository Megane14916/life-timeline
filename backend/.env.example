# Optional example for local development. The application reads the process
# environment; load this value in your shell before starting the Backend.
LIFE_TIMELINE_DATA_DIR=C:/Users/<username>/AppData/Local/life-timeline
